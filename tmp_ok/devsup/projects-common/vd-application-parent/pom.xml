<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <artifactId>vd-application-parent</artifactId>
    <version>2.3.6-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>DEVSUPPORT - Application Parent</name>

    <parent>
        <groupId>ch.vd.devsupport</groupId>
        <artifactId>vd-dependencies</artifactId>
        <version>2.3.6-SNAPSHOT</version>
        <relativePath>../vd-dependencies</relativePath>
    </parent>

    <profiles>
        <profile>
            <id>nof</id>
            <properties>
                <build.nofront>true</build.nofront>
                <skipTestsFront>true</skipTestsFront>
            </properties>
        </profile>
        <profile>
            <id>ci</id>
            <properties>
                <npm.install.argument>ci</npm.install.argument>
            </properties>
        </profile>
        <profile>
            <id>assembly</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <descriptors>
                                <descriptor>assembly.xml</descriptor>
                            </descriptors>
                            <appendAssemblyId>false</appendAssemblyId>
                            <tarLongFileMode>posix</tarLongFileMode>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>default-install</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- After creating the assembly, we only keep the .tar.gz, otherwise, the deploy goal will upload both the jar and the assembly -->
                    <!-- Using deploy:deploy-file is not convenient, because we cannot cleanly choose the repository type, ie SNAPSHOT vs STABLE to which we want to upload -->
                    <plugin>
                        <artifactId>maven-clean-plugin</artifactId>
                        <executions>
                            <execution>
                                <!-- we can't override this id -->
                                <id>default-clean</id>
                                <phase>install</phase>
                                <goals>
                                    <goal>clean</goal>
                                </goals>
                                <configuration>
                                    <excludeDefaultDirectories>true</excludeDefaultDirectories>
                                    <filesets>
                                        <fileset>
                                            <directory>${project.build.directory}</directory>
                                            <excludes>
                                                <exclude>${project.artifactId}-${project.version}.tar.gz</exclude>
                                            </excludes>
                                        </fileset>
                                    </filesets>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>nexus</id>
            <properties>
                <release.goals>clean deploy</release.goals>
                <devex-maven-plugin.phase>deploy</devex-maven-plugin.phase>
            </properties>
            <distributionManagement>
                <repository>
                    <id>nexus-dgnsi-server</id>
                    <name>Nexus DGNSI</name>
                    <url>${nexus.url}/app-${application.repository}-stable</url>
                </repository>
                <snapshotRepository>
                    <id>nexus-dgnsi-server</id>
                    <name>Nexus DGNSI</name>
                    <url>${nexus.url}/app-${application.repository}-snapshots</url>
                    <uniqueVersion>false</uniqueVersion>
                </snapshotRepository>
            </distributionManagement>
            <build>
                <pluginManagement>
                    <plugins>
                        <plugin>
                            <groupId>org.apache.maven.plugins</groupId>
                            <artifactId>maven-deploy-plugin</artifactId>
                            <configuration>
                                <allowIncompleteProjects>true</allowIncompleteProjects>
                            </configuration>
                        </plugin>
                    </plugins>
                </pluginManagement>
            </build>
        </profile>
        <profile>
            <id>devex.release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>create</id>
                                <phase>${devex-maven-plugin.phase}</phase>
                                <goals>
                                    <goal>create-release</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>devex.in</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>deploy</id>
                                <phase>${devex-maven-plugin.phase}</phase>
                                <goals>
                                    <goal>deploy</goal>
                                </goals>
                                <configuration>
                                    <environment>IN</environment>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>platform-windows</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <properties>
                <node.packagemanager.executable>npm.cmd</node.packagemanager.executable>
            </properties>
        </profile>
        <profile>
            <id>angular</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>exec-front-install</id>
                                <phase>prepare-package</phase>
                                <configuration>
                                    <workingDirectory>${exec-maven-plugin.workingDirectory.front}</workingDirectory>
                                    <executable>${node.packagemanager.executable}</executable>
                                    <arguments>
                                        <argument>${npm.install.argument}</argument>
                                    </arguments>
                                    <skip>${build.nofront}</skip>
                                </configuration>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>exec-ngcli-tests</id>
                                <phase>prepare-package</phase>
                                <configuration>
                                    <workingDirectory>${exec-maven-plugin.workingDirectory.front}</workingDirectory>
                                    <executable>${node.packagemanager.executable}</executable>
                                    <arguments>
                                        <argument>run</argument>
                                        <argument>${angular.test.argument}</argument>
                                    </arguments>
                                    <skip>${skipTestsFront}</skip>
                                </configuration>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>exec-ngcli-lint</id>
                                <phase>prepare-package</phase>
                                <configuration>
                                    <workingDirectory>${exec-maven-plugin.workingDirectory.front}</workingDirectory>
                                    <executable>${node.packagemanager.executable}</executable>
                                    <arguments>
                                        <argument>run</argument>
                                        <argument>${angular.lint.argument}</argument>
                                    </arguments>
                                    <skip>${skipTestsFront}</skip>
                                </configuration>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>exec-ngcli-build</id>
                                <phase>prepare-package</phase>
                                <configuration>
                                    <workingDirectory>${exec-maven-plugin.workingDirectory.front}</workingDirectory>
                                    <executable>${node.packagemanager.executable}</executable>
                                    <arguments>
                                        <argument>run</argument>
                                        <argument>${angular.buildprod.argument}</argument>
                                    </arguments>
                                    <skip>${build.nofront}</skip>
                                </configuration>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <configuration>
                            <encoding>${project.build.sourceEncoding}</encoding>
                        </configuration>
                        <executions>
                            <execution>
                                <id>exec-copy-front-noindex</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>
                                        ${target.classes}/${target.classes.publicOrStatic}/${static.final.outputDirectory}
                                    </outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${front.dist}/${front.dist.package.outputDirectory}</directory>
                                            <filtering>false</filtering>
                                            <excludes>
                                                <exclude>index.html</exclude>
                                                <!-- can be removed in a major version when all is migrated to APP_BASE_HREF -->
                                                <exclude>${front.dist.static.outputDirectory}/**/*.*</exclude>
                                            </excludes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                            <execution>
                                <id>exec-copy-front-onlyindex</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${target.classes}/${target.classes.publicOrStatic}
                                    </outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${front.dist}/${front.dist.package.outputDirectory}</directory>
                                            <filtering>false</filtering>
                                            <includes>
                                                <include>index.html</include>
                                                <!-- can be removed in a major version when all is migrated to APP_BASE_HREF -->
                                                <include>${front.dist.static.outputDirectory}/**/*.*</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <finalName>${assembly.finalName}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>buildnumber-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>create-timestamp</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>version.txt</exclude>
                </excludes>
                <filtering>false</filtering>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>version.txt</include>
                </includes>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>ch.vd.devsupport</groupId>
                <artifactId>vd-spring-boot-starter</artifactId>
                <version>${vd-spring-boot-starter.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <properties>
        <vd-spring-boot-starter.version>2.3.6-SNAPSHOT</vd-spring-boot-starter.version>

        <assembly.finalName>${project.artifactId}-${project.version}</assembly.finalName>
        <maven.deploy.skip>false</maven.deploy.skip>

        <!-- front build configuration -->
        <exec-maven-plugin.workingDirectory.front>${basedir}/front</exec-maven-plugin.workingDirectory.front>
        <build.nofront>false</build.nofront>
        <skipTestsFront>false</skipTestsFront>

        <node.packagemanager.executable>npm</node.packagemanager.executable>
        <npm.install.argument>install</npm.install.argument>

        <target.classes>${project.build.directory}/classes</target.classes>
        <target.classes.publicOrStatic>public</target.classes.publicOrStatic>
        <static.final.outputDirectory>static</static.final.outputDirectory>
        <front.dist>front/dist</front.dist>
        <front.dist.package.outputDirectory>package</front.dist.package.outputDirectory>

        <!-- can be removed in a major version when all is migrated to APP_BASE_HREF -->
        <front.dist.static.outputDirectory>static</front.dist.static.outputDirectory>

        <angular.test.argument>test:ci</angular.test.argument>
        <angular.lint.argument>lint</angular.lint.argument>
        <angular.buildprod.argument>build:prod</angular.buildprod.argument>

        <!-- maven release plugin configuration for applications -->
        <devex.profiles>devex.release,devex.in</devex.profiles>
        <devex-maven-plugin.phase>package</devex-maven-plugin.phase>
        <upload.profile>nexus</upload.profile>
        <assembly.profile>assembly</assembly.profile>
        <!-- we keep this as is for BC concerns, althought it's now overrididden in most *devsup* pipeline -->
        <!-- some projects may rely on the fact that perform skip tests-->
        <release.profiles>not,${assembly.profile},${upload.profile},${devex.profiles}</release.profiles>
        <release.pushChanges>true</release.pushChanges>
    </properties>
</project>
