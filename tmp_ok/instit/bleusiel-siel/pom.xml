<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <artifactId>siel</artifactId>
    <groupId>ch.vd.institutions.siel</groupId>
    <version>9.2.2-SNAPSHOT</version>
    <name>prj_ev-bleusiel Siel</name>
    <packaging>pom</packaging>

    <description>SIEL POM
        Used to regroup siel back and siel assembly into one project to ease release
    </description>

    <modules>
        <module>siel-back</module>
        <module>siel-assembly</module>
    </modules>

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
        <dependency-check-maven.version>7.4.4</dependency-check-maven.version>
        <sonar.dependencyCheck.jsonReportPath>target/dependency-check-report.json</sonar.dependencyCheck.jsonReportPath>
        <sonar.dependencyCheck.htmlReportPath>target/dependency-check-report.html</sonar.dependencyCheck.htmlReportPath>
    </properties>

    <scm>
        <connection>${profile.git.connection}</connection>
        <tag>HEAD</tag>
    </scm>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <!-- extract version information without SNAPSHOT -->
                    <execution>
                        <id>app-version</id>
                        <goals>
                            <goal>regex-property</goal>
                        </goals>
                        <configuration>
                            <name>product.version</name>
                            <value>${project.version}</value>
                            <regex>(-SNAPSHOT)?$</regex>
                            <replacement />
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>3.0.0</version>
                <configuration>
                    <goals>clean deploy</goals>
                    <pushChanges>true</pushChanges>
                    <arguments>-Dmaven.javadoc.skip=true</arguments>
                    <localCheckout>true</localCheckout>
                    <autoVersionSubmodules>true</autoVersionSubmodules>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>elca</id>
            <properties>
                <profile.git.connection>scm:git:file:.</profile.git.connection>
            </properties>
        </profile>
        <profile>
            <id>dgnsi</id>
            <properties>
                <profile.git.connection>scm:git:ssh://git@bitbucket.etat-de-vaud.ch/instit/bleusiel-siel.git
                </profile.git.connection>
            </properties>
        </profile>
        <!-- can be removed when using the latest vd-application-parent -->
        <profile>
            <id>owasp-dependency-check</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>${dependency-check-maven.version}</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>aggregate</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <formats>
                                <format>json</format>
                                <format>html</format>
                            </formats>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
