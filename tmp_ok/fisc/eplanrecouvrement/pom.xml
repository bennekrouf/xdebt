<?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ch.vd.cyber.fisc</groupId>
    <artifactId>eplanrecouvrement</artifactId>
    <version>24.1.1-SNAPSHOT</version>
    <packaging>war</packaging>

    <name>EplanRecouvrement - Prestation Application</name>

    <parent>
        <groupId>ch.vd.cyber.parent</groupId>
        <artifactId>application-parent</artifactId>
        <version>13.3.0</version>
        <relativePath />
    </parent>

    <scm>
        <developerConnection>scm:git:ssh://git@bitbucket.etat-de-vaud.ch/fisc/eplanrecouvrement.git</developerConnection>
      <tag>HEAD</tag>
    </scm>

    <dependencies>
        <dependency>
            <groupId>ch.vd.cyber</groupId>
            <artifactId>starter-prestation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>ch.vd.fiscalite.tam.logger</groupId>
            <artifactId>tam-utils-commons</artifactId>
            <version>${tam-utils.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.vd.fiscalite.tam.logger</groupId>
            <artifactId>tam-utils-logback</artifactId>
            <version>${tam-utils.version}</version>
        </dependency>

        <dependency>
            <groupId>ch.vd.cyber.fisc</groupId>
            <artifactId>fiscalite-cyber-be</artifactId>
            <version>${fiscalite-cyber-be.version}</version>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>ch.vd.cybersdk</groupId>
            <artifactId>prestations-be</artifactId>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>${maven-resources-plugin.version}</version>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
                <executions>
                    <execution>
                        <id>copy-build-infos</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/classes</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/resources</directory>
                                    <includes>
                                        <include>build-infos.txt</include>
                                    </includes>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jvnet.jaxb2.maven2</groupId>
                <artifactId>maven-jaxb2-plugin</artifactId>
                <configuration>
                    <strict>false
                    </strict> <!-- see https://github.com/highsource/maven-jaxb2-plugin/wiki/Catalogs-in-Strict-Mode -->
                    <schemaDirectory>${basedir}/src/main/resources/xsd/</schemaDirectory>
                    <args>
                        <arg>-extension</arg>
                        <arg>-Xvalue-constructor</arg>
                    </args>
                </configuration>

                <executions>
                    <execution>
                        <id>document-schema-generate</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <schemaIncludes>
                                <include>documentDematPlanRecouvrement-1.xsd</include>
                            </schemaIncludes>
                            <generatePackage>ch.vd.eplanrecouvrement.document</generatePackage>
                            <generateDirectory>${project.build.directory}/generated-sources/xjc1</generateDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>entetes-schema-generate</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <schemaIncludes>
                                <include>entetesPrestation-1.xsd</include>
                            </schemaIncludes>
                            <generatePackage>ch.vd.eplanrecouvrement.entetes</generatePackage>
                            <generateDirectory>${project.build.directory}/generated-sources/xjc2</generateDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>pdf-schema-generate</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <schemaIncludes>
                                <include>Cyber_FichierImpression_1.9.0.xsd</include>
                            </schemaIncludes>
                            <generatePackage>ch.vd.eplanrecouvrement.pdf</generatePackage>
                            <generateDirectory>${project.build.directory}/generated-sources/xjc3</generateDirectory>
                        </configuration>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.jvnet.jaxb2_commons</groupId>
                        <artifactId>jaxb2-value-constructor</artifactId>
                        <version>${jaxb2-value-constructor.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

    <properties>
        <devex.configTag>24.02.2</devex.configTag>
        <tam-utils.version>1.0.10</tam-utils.version>
        <fiscalite-cyber-be.version>${project.parent.version}</fiscalite-cyber-be.version>
    </properties>

</project>