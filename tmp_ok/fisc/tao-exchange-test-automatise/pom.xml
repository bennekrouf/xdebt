<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <parent>
    <groupId>ch.vd.dfin.tao</groupId>
    <artifactId>tao-parent</artifactId>
    <version>3.0.15</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>
  <artifactId>tao-exchange-test-automatise</artifactId>
  <version>8.1.3-SNAPSHOT</version>
  <packaging>jar</packaging>
  <name>tao-exchange-test-automatise</name>

  <properties>
    <classifier.config>test</classifier.config>
    <test.skipping>true</test.skipping>
    <compile.test.skipping>true</compile.test.skipping>
    <project.encoding>UTF-8</project.encoding>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <lib.activemq.version>5.10.0</lib.activemq.version>
    <lib.spring.version>4.3.22.RELEASE</lib.spring.version>
    <slf4j-api.version>1.7.22</slf4j-api.version>
  </properties>

  <build>

    <testResources>
      <testResource>
        <directory>${project.basedir}/src/test/resources</directory>
      </testResource>
      <testResource>
        <!-- On renseignement aussi le répertoire java pour pouvoir récupérer tous les
          fichiers .xml ou autre nécessaires à l'exécution des tests -->
        <directory>${project.basedir}/src/test/java</directory>
        <excludes>
          <exclude>**/*.java</exclude>
        </excludes>
      </testResource>
    </testResources>

    <plugins>
      <plugin>
         <artifactId>maven-compiler-plugin</artifactId>
         <configuration>
             <encoding>${project.encoding}</encoding>
             <source>11</source>
             <target>11</target>
         </configuration>
     </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <!-- Définition par défaut du plugin pour que les tests ne soient
        pas exécutés lors d'un build du projet -->
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>${test.skipping}</skip>
        </configuration>
        <executions>
          <execution>
            <id>default-test</id>
            <goals>
              <goal>test</goal>
            </goals>
            <phase>test</phase>
          </execution>
        </executions>
      </plugin>
      <!-- Définition par défaut du plugin pour que la compilation des
        classes de test ne soient pas exécutés lors d'un build du projet -->
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <skip>${compile.test.skipping}</skip>
        </configuration>
        <executions>
          <execution>
            <id>default-testCompile</id>
            <goals>
              <goal>testCompile</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

  <profiles>
    <profile>
      <!-- Profil pour l'intégration continue -->
      <id>profilJenkins</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <properties>
        <!--  Les tests ne sont pas skippés -->
        <test.skipping>false</test.skipping>
        <!-- La compilation des classes test n'est pas skippée -->
        <compile.test.skipping>false</compile.test.skipping>
      </properties>
      <!--
      <dependencies>
          <dependency>
              <groupId>${project.groupId}</groupId>
              <artifactId>tao-exchange</artifactId>
              <version>${project.version}</version>
              <type>war</type>
          </dependency>
      </dependencies>
      -->
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <executions>
              <execution>
                <id>default-test</id>
                <goals>
                  <goal>test</goal>
                </goals>
                <phase>test</phase>
                <configuration>
                  <runOrder>alphabetical</runOrder>
                  <!-- Ajout du forkMode pour permettre la création d'un process pour chaque test
                      et par conséquence le re-loader du context pour chaque test -->
                  <!-- >forkMode>false</forkMode -->
                  <!-- Ajout de variables utilisables dans le code java pour savoir que le test est
                      lancé depuis jenkins  -->
                  <systemPropertyVariables>
                    <!--  variable qui indique le path du filestore -->
                    <pathFilestore>filestore</pathFilestore>
                    <!--  variable qui indique le path du répertoire de config -->
                    <appDir>config</appDir>
                  </systemPropertyVariables>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <!-- Profil pour lancer depuis la console en local -->
      <id>profilConsole</id>
      <activation>
        <activeByDefault>false</activeByDefault>
      </activation>
      <properties>
        <!--  Les tests ne sont pas skippés -->
        <test.skipping>false</test.skipping>
        <!-- La compilation des classes test n'est pas skippée -->
        <compile.test.skipping>false</compile.test.skipping>
      </properties>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <executions>
              <execution>
                <id>default-test</id>
                <goals>
                  <goal>test</goal>
                </goals>
                <phase>test</phase>
                <configuration>
                  <runOrder>alphabetical</runOrder>
                  <!-- Ajout du forkMode pour permettre la création d'un process pour chaque test
                    et par conséquence le re-loader du context pour chaque test -->
                  <!-- >forkMode>false</forkMode -->
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <!-- Profil exécuté par défaut (via eclipse) -->
      <id>default</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <executions>
              <execution>
                <id>default-test</id>
                <goals>
                  <goal>test</goal>
                </goals>
                <phase>test</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <dependencies>
    <dependency>
      <groupId>ch.vd.dfin.tao</groupId>
      <artifactId>tao-exchange</artifactId>
      <version>21R2.A.10</version>
    </dependency>
    <!-- Dépendences de test -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-test</artifactId>
      <version>${lib.spring.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.easymock</groupId>
      <artifactId>easymock</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.easymock</groupId>
      <artifactId>easymockclassextension</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.12</version>
      <!-- Version surchargée par rapport au parent, avec JUnit 4.4, Surefire
        essaye de tester des classes nommées *Test qui ne possèdent pas de méthodes
        de test et lève des exceptions "No runnable method". -->
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>xmlunit</groupId>
      <artifactId>xmlunit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.googlecode.java-diff-utils</groupId>
      <artifactId>diffutils</artifactId>
      <version>1.2.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>weblogic</groupId>
      <artifactId>wlthint3client</artifactId>
      <version>12.1.3</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>ch.vd.dfin.tao</groupId>
      <artifactId>tao-test-util</artifactId>
      <version>1.1.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>ch.vd.dfin.tao</groupId>
      <artifactId>tao-test-automatise</artifactId>
      <version>2.1.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jencks</groupId>
      <artifactId>jencks</artifactId>
      <version>2.2</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <groupId>org.apache.geronimo.components</groupId>
          <artifactId>geronimo-transaction</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.jencks</groupId>
      <artifactId>jencks-amqpool</artifactId>
      <version>2.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-entitymanager</artifactId>
      <version>3.6.7.Final</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-annotations</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-jdbc</artifactId>
      <version>${lib.spring.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-orm</artifactId>
      <version>${lib.spring.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.jms</groupId>
      <artifactId>jms</artifactId>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j-api.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <version>${slf4j-api.version}</version>
    </dependency>

  </dependencies>

  <scm>
    <connection>scm:svn:https://svn.etat-de-vaud.ch/SVN_ACI/taotra/tunk/tao-exchange-test-automatise/</connection>
    <developerConnection>scm:svn:https://svn.etat-de-vaud.ch/SVN_ACI/taotra/tunk/tao-exchange-test-automatise/
    </developerConnection>
  </scm>
</project>



