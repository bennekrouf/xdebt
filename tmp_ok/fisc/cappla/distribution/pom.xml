<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.vd.fiscalite</groupId>
        <artifactId>cappla-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>cappla</artifactId>
    <packaging>pom</packaging>

    <name>Distribution</name>
    <description>Construit le livrable de l'application</description>

    <properties>
        <!-- Ensemble des properties pour le déploiement de l'application -->
        <devex-maven-plugin.version>1.0.19</devex-maven-plugin.version>
        <file.mode.release>0644</file.mode.release>
        <build.userId/>
        <devex.configTag/>
        <devex.releaseVersion>${project.version}</devex.releaseVersion>
        <devex.deployUnit>cappla</devex.deployUnit>
        <devex.deployTarget>tomcat9</devex.deployTarget>
        <devex.deployEnv>IN</devex.deployEnv>
    </properties>

    <dependencies>
        <dependency>
            <groupId>ch.vd.fiscalite</groupId>
            <artifactId>cappla-web</artifactId>
            <version>${project.version}</version>
            <type>war</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <configuration>
                    <artifactItems>
                        <artifactItem>
                            <groupId>ch.vd.fiscalite</groupId>
                            <artifactId>cappla-web</artifactId>
                            <version>${project.version}</version>
                            <type>war</type>
                        </artifactItem>
                    </artifactItems>
                </configuration>
                <executions>
                    <execution>
                        <id>copy-dependencies-distribution</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/release.xml</descriptor>
                    </descriptors>
                    <finalName>${project.artifactId}-${project.version}</finalName>
                    <appendAssemblyId>false</appendAssemblyId>
                    <tarLongFileMode>posix</tarLongFileMode>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>devex.release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <version>${devex-maven-plugin.version}</version>
                        <configuration>
                            <deployTarget>${devex.deployTarget}</deployTarget>
                            <configTag>${devex.configTag}</configTag>
                            <deployUnit>${devex.deployUnit}</deployUnit>
                        </configuration>
                        <executions>
                            <execution>
                                <id>release</id>
                                <phase>install</phase>
                                <goals>
                                    <goal>create-release</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>devex.deploy</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <version>${devex-maven-plugin.version}</version>
                        <configuration>
                            <deployTarget>${devex.deployTarget}</deployTarget>
                            <configTag>${devex.configTag}</configTag>
                            <deployUnit>${devex.deployUnit}</deployUnit>
                            <environment>${devex.deployEnv}</environment>
                            <releaseVersion>${project.version}</releaseVersion>
                        </configuration>
                        <executions>
                            <execution>
                                <id>deploy</id>
                                <phase>deploy</phase>
                                <goals>
                                    <goal>deploy</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
