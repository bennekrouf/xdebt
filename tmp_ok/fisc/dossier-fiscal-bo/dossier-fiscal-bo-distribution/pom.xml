<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <groupId>ch.vd.dfin.tao.dossier-fiscal.bo</groupId>
        <artifactId>dossier-fiscal-bo-app</artifactId>
        <version>24R2.C.5-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>dossier-fiscal-bo</artifactId>
    <packaging>pom</packaging>
    <name>dossier-fiscal-bo-distribution</name>

    <properties>
        <!-- Distribution -->
        <dist.app.link.name>${project.artifactId}-${project.version}</dist.app.link.name>
        <application.version>${project.version}</application.version>
        <application.buildTime>${maven.build.timestamp}</application.buildTime>

        <devex.deployEnv>IN</devex.deployEnv>
        <devex.configTag>master</devex.configTag>
        <devex.releaseVersion>${project.version}</devex.releaseVersion>
        <devex.deployUnit>dossierfiscalbo</devex.deployUnit>
        <devex.deployTarget>Tomcat9RHEL8</devex.deployTarget>

    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <!-- The Assembly Plugin for Maven is primarily intended to allow users to aggregate the project output along with its dependencies, modules, site documentation, and other files into a single distributable archive -->
                <plugin>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>assembly-release</id>
                            <phase>install</phase>
                            <configuration>
                                <appendAssemblyId>false</appendAssemblyId>
                                <finalName>${dist.app.link.name}</finalName>
                                <descriptors>
                                    <descriptor>${basedir}/src/main/assembly/dossier-fiscal-bo-release.xml</descriptor>
                                </descriptors>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <execution>
                        <id>assembly-release</id>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>build-delivery-devex</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>assembly-release</id>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Création de la release dans devex -->
        <profile>
            <!-- pour l'utilisation du plugin devex : les varaible d'environnement doivent être setter : déjà effectué sur jenkins
              DEVEX_URL=http://outils-devex-ws.etat-de-vaud.ch/outils/devex
              DEVEX_USERNAME=< IUP>
              DEVEX_PASSWORD=<pwd ADACV>
            -->
            <id>devex.release</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <skip>false</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <version>${devex-maven-plugin.version}</version>
                        <configuration>
                            <configTag>${devex.configTag}</configTag>
                            <deployUnit>${devex.deployUnit}</deployUnit>
                            <deployTarget>${devex.deployTarget}</deployTarget>
                        </configuration>
                        <executions>
                            <execution>
                                <id>release</id>
                                <phase>install</phase>
                                <goals>
                                    <goal>create-release</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Déploiment de la release dans devex -->
        <profile>
            <!-- pour l'utilisation du plugin devex : les varaible d'environnement doivent être setter : déjà effectué sur jenkins
              DEVEX_URL=http://outils-devex-ws.etat-de-vaud.ch/outils/devex
              DEVEX_USERNAME=< IUP>
              DEVEX_PASSWORD=<pwd ADACV>
            -->
            <id>devex.deploy</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <version>${devex-maven-plugin.version}</version>
                        <configuration>
                            <configTag>${devex.configTag}</configTag>
                            <deployUnit>${devex.deployUnit}</deployUnit>
                            <deployTarget>${devex.deployTarget}</deployTarget>
                            <environment>${devex.deployEnv}</environment>
                            <releaseVersion>${project.version}</releaseVersion>
                        </configuration>
                        <executions>
                            <execution>
                                <id>deploy</id>
                                <phase>deploy</phase>
                                <goals>
                                    <goal>deploy</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
