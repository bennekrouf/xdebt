<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ch.vd.dfin.tao.pom</groupId>
    <artifactId>tao-pom</artifactId>
    <version>1.1.23</version>
  </parent>

  <!-- ###### SCM : Source Control Management ###### -->
  <!-- définition de l'url de connection au serveur GIT pour le projet -->
  <scm>
    <connection>scm:git:ssh://git@bitbucket.etat-de-vaud.ch/fisc/tao-parseur-bible.git</connection>
    <developerConnection>scm:git:ssh://git@bitbucket.etat-de-vaud.ch/fisc/tao-parseur-bible.git</developerConnection>
    <url>https://portail.etat-de-vaud.ch/outils/git/projects/FISC/repos/tao-parseur-bible</url>
    <tag>HEAD</tag>
  </scm>

  <groupId>ch.vd.dfin.tao.utils</groupId>
  <artifactId>tao-parseur-bible</artifactId>
  <name>tao-parseur-bible</name>
  <version>1.15.9-SNAPSHOT</version>

  <properties>
    <!-- Pas d'analyse Sonar sur ce projet
    (beaucoup trop de verrues, de duplication de code, de constantes en dur, et autres bad-practices) -->
    <sonar.skip>true</sonar.skip>
    <tao-core.version>22R2.A.1</tao-core.version>
    <oracle.version>12.1.0.2.0</oracle.version>

    <spring-framework.version>5.2.20.RELEASE</spring-framework.version>
  </properties>

  <dependencies>

    <!-- tao-core -->
    <dependency>
      <groupId>ch.vd.dfin.tao.core</groupId>
      <artifactId>tao-core-api</artifactId>
      <version>${tao-core.version}</version>
    </dependency>
    <dependency>
      <groupId>ch.vd.dfin.tao.core</groupId>
      <artifactId>tao-engine</artifactId>
      <version>${tao-core.version}</version>
    </dependency>
    <dependency>
      <groupId>ch.vd.dfin.tao.core</groupId>
      <artifactId>tao-core-test</artifactId>
      <scope>test</scope>
      <version>${tao-core.version}</version>
    </dependency>

    <!-- Spring -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-context</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-context-support</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-jdbc</artifactId>
    </dependency>

    <!-- DataBase -->
    <dependency>
      <groupId>oracle.jdbc</groupId>
      <artifactId>ojdbc7</artifactId>
    </dependency>

    <!-- Utils -->
    <dependency>
      <groupId>commons-beanutils</groupId>
      <artifactId>commons-beanutils</artifactId>
    </dependency>
    <dependency>
      <groupId>commons-codec</groupId>
      <artifactId>commons-codec</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
    </dependency>
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>

    <!-- Apache POI -->
    <dependency>
      <groupId>org.apache.poi</groupId>
      <artifactId>poi</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.poi</groupId>
      <artifactId>poi-ooxml</artifactId>
    </dependency>

  </dependencies>
</project>
