<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>ch.vd.sipf.bof</groupId>
        <artifactId>sipfbof</artifactId>
        <version>1.3.7-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>seipslave</artifactId>
    <name>distribution-slave</name>
    <packaging>pom</packaging>

    <properties>
        <!-- Propriétés devex pour le déploiement  -->
        <devex.deployEnv>IN</devex.deployEnv>
        <devex.configTag>slave</devex.configTag>
        <devex.releaseVersion>${project.version}</devex.releaseVersion>
        <devex.deployUnit.slave1>seipslave01</devex.deployUnit.slave1>
        <devex.deployUnit.slave2>seipslave02</devex.deployUnit.slave2>
        <devex.deployTarget.slave1>Tomcat</devex.deployTarget.slave1>
        <devex.deployTarget.slave2>Tomcat</devex.deployTarget.slave2>
    </properties>

    <profiles>
        <profile>
            <!-- pour le déploiement en IN -->
            <id>env.in</id>
            <properties>
                <assembly.env>IN</assembly.env>
            </properties>
            <build>
                <plugins>
                    <!-- Pour générer le fichier pour le CEI -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <version>${maven-assembly-plugin.version}</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <descriptors>
                                <descriptor>assembly_slave.xml</descriptor>
                            </descriptors>
                            <!--                            <finalName>slave</finalName>-->
                            <!--                            <finalName>${assembly.slave.finalName}</finalName>-->
                            <appendAssemblyId>false</appendAssemblyId>
                            <tarLongFileMode>gnu</tarLongFileMode>
                            <outputDirectory>../target</outputDirectory>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>build-delivery-devex</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <skip>false</skip>
                        </configuration>
                    </plugin>
                    <!-- Pour générer le fichier pour le CEI -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <version>${maven-assembly-plugin.version}</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <descriptors>
                                <descriptor>assembly_slave.xml</descriptor>
                            </descriptors>
                            <finalName>${assembly.slave.finalName}</finalName>
                            <appendAssemblyId>false</appendAssemblyId>
                            <tarLongFileMode>gnu</tarLongFileMode>
                            <outputDirectory>target</outputDirectory>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Création de la release dans devex -->
        <profile>
            <!-- pour l'utilisation du plugin devex : les variables d'environnement doivent être setter : déjà effectué sur jenkins
              DEVEX_URL=http://outils-devex-ws.etat-de-vaud.ch/outils/devex
              DEVEX_USERNAME=< IUP>
              DEVEX_PASSWORD=<pwd ADACV>
            -->
            <id>devex.release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <version>${devex-maven-plugin.version}</version>
                        <configuration>
                            <configTag>${devex.configTag}</configTag>
                        </configuration>
                        <executions>
                            <execution>
                                <id>release.slave1</id>
                                <phase>install</phase>
                                <configuration>
                                    <deployUnit>${devex.deployUnit.slave1}</deployUnit>
                                    <deployTarget>${devex.deployTarget.slave1}</deployTarget>
                                </configuration>
                                <goals>
                                    <goal>create-release</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>release.slave2</id>
                                <phase>install</phase>
                                <configuration>
                                    <deployUnit>${devex.deployUnit.slave2}</deployUnit>
                                    <deployTarget>${devex.deployTarget.slave2}</deployTarget>
                                </configuration>
                                <goals>
                                    <goal>create-release</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Déploiment de la release dans devex -->
        <profile>
            <!-- pour l'utilisation du plugin devex : les variables d'environnement doivent être setter : déjà effectué sur jenkins
              DEVEX_URL=http://outils-devex-ws.etat-de-vaud.ch/outils/devex
              DEVEX_USERNAME=< IUP>
              DEVEX_PASSWORD=<pwd ADACV>
            -->
            <id>devex.deploy</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ch.vd.devex</groupId>
                        <artifactId>devex-maven-plugin</artifactId>
                        <version>${devex-maven-plugin.version}</version>
                        <configuration>
                            <configTag>${devex.configTag}</configTag>
                            <environment>${devex.deployEnv}</environment>
                            <releaseVersion>${project.version}</releaseVersion>
                        </configuration>
                        <executions>
                            <execution>
                                <id>deploy.slave1</id>
                                <phase>deploy</phase>
                                <configuration>
                                    <deployUnit>${devex.deployUnit.slave1}</deployUnit>
                                    <deployTarget>${devex.deployTarget.slave1}</deployTarget>
                                </configuration>
                                <goals>
                                    <goal>deploy</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>deploy.slave2</id>
                                <phase>deploy</phase>
                                <configuration>
                                    <deployUnit>${devex.deployUnit.slave2}</deployUnit>
                                    <deployTarget>${devex.deployTarget.slave2}</deployTarget>
                                </configuration>
                                <goals>
                                    <goal>deploy</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
