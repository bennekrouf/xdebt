<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>pom</packaging>
    <modules>
        <module>batch-master</module>
        <module>batch-slave</module>
        <module>batch-refresh</module>
        <module>slave-process</module>
        <module>modele</module>
        <module>util</module>
        <module>ProductionFlux</module>
        <module>distribution-master</module>
        <module>distribution-slave</module>
        <module>distribution-refresh</module>
        <module>mock-esb</module>
    </modules>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.5.2</version>
        <relativePath /> <!-- lookup parent from repository -->
    </parent>
    <groupId>ch.vd.sipf.bof</groupId>
    <artifactId>sipfbof</artifactId>
    <version>1.3.7-SNAPSHOT</version>
    <name>sipfbof</name>
    <description>Interface between SIPF and BO Fiscal (BOF)</description>

    <organization>
        <name>Cross Systems SA</name>
        <url>www.cross-engineering.ch</url>
    </organization>

    <scm>
        <connection>${scm.url}</connection>
        <developerConnection>${scm.url}</developerConnection>
        <tag>1.3.4.1</tag>
    </scm>

    <properties>
        <revision>1.3.6-SNAPSHOT</revision>
        <java.version>11</java.version>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <sonar.projectName>SEIP : Interface SIPF - BOF</sonar.projectName>
        <devex.master.app.name>seipmaster</devex.master.app.name>
        <assembly.master.finalName>${devex.master.app.name}-${project.version}</assembly.master.finalName>
        <devex.slave.app.name>seipslave</devex.slave.app.name>
        <assembly.slave.finalName>${devex.slave.app.name}-${project.version}</assembly.slave.finalName>
        <devex.refresh.app.name>seiprefresh</devex.refresh.app.name>
        <assembly.refresh.finalName>${devex.refresh.app.name}-${project.version}</assembly.refresh.finalName>
        <maven-assembly-plugin.version>2.5.3</maven-assembly-plugin.version>
        <cei-maven-plugin.version>1.0.3</cei-maven-plugin.version>
        <maven.javadoc.failOnError>false</maven.javadoc.failOnError>
        <jacoco-version>0.8.12</jacoco-version>
        <mockito.version>3.2.4</mockito.version>
        <powermock.version>2.0.5</powermock.version>
        <devex-maven-plugin.version>1.0.19</devex-maven-plugin.version>
        <unireg-jaxb2-version>7.21</unireg-jaxb2-version>
        <!-- PFIS-1657 -->
        <devex.autoDeploy>true</devex.autoDeploy>
        <devex.deployEnv>IN</devex.deployEnv>
        <devex.applicationVersion>master</devex.applicationVersion>
        <devex.configTag>24_13</devex.configTag>
        <!-- PFIS-1657 -->
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-batch</artifactId>
            <exclusions>
                <exclusion>
                    <artifactId>log4j-api</artifactId>
                    <groupId>org.apache.logging.log4j</groupId>
                </exclusion>
                <exclusion>
                    <artifactId>log4j-to-slf4j</artifactId>
                    <groupId>org.apache.logging.log4j</groupId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-activemq</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
            <version>2.7.18</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.integration</groupId>
            <artifactId>spring-integration-jms</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-oxm</artifactId>
            <version>5.2.14.RELEASE</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.batch</groupId>
            <artifactId>spring-batch-integration</artifactId>
        </dependency>
        <dependency>
            <groupId>com.oracle.database.jdbc</groupId>
            <artifactId>ojdbc10</artifactId>
            <version>19.11.0.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.hsqldb</groupId>
            <artifactId>hsqldb</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <!-- this version, so as to compile with java21 -->
            <version>1.18.30</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-module-junit4</artifactId>
            <version>${powermock.version}</version>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-module-junit4-rule-agent</artifactId>
            <version>${powermock.version}</version>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-module-javaagent</artifactId>
            <version>${powermock.version}</version>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-api-mockito2</artifactId>
            <version>${powermock.version}</version>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-classloading-xstream</artifactId>
            <version>${powermock.version}</version>
        </dependency>
        <dependency>
            <groupId>javax.cache</groupId>
            <artifactId>cache-api</artifactId>
            <version>1.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.ehcache</groupId>
            <artifactId>ehcache</artifactId>
            <version>3.10.8</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>env.in</id>
            <modules>
                <module>distribution-master</module>
                <module>distribution-slave</module>
                <module>distribution-refresh</module>
            </modules>
        </profile>

        <profile>
            <id>env.cei</id>
            <modules>
                <module>distribution-master</module>
                <module>distribution-slave</module>
                <module>distribution-refresh</module>
            </modules>
        </profile>

        <!-- Profil DSI Vaud, définition propre à l'usage du projet dans le contexte de la DGNSI -->
        <profile>
            <id>dsi</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <scm.url>scm:git:ssh://git@bitbucket.etat-de-vaud.ch/fisc/seip.git</scm.url>
            </properties>

            <distributionManagement>
                <repository>
                    <id>nexus.releases</id>
                    <name>Nexus releases</name>
                    <url>${nexus.url}/app-${application.repository}-stable</url>
                </repository>
                <snapshotRepository>
                    <id>nexus.snapshots</id>
                    <name>Nexus snaphots</name>
                    <url>${nexus.url}/app-${application.repository}-snapshots</url>
                    <uniqueVersion>false</uniqueVersion>
                </snapshotRepository>
            </distributionManagement>
        </profile>

        <!-- Fournisseur de la solution, définition propre à l'usage du projet dans le contexte de CROSS -->
        <profile>
            <id>fournisseur</id>
            <properties>
                <scm.url>scm:git:https://git.cross-systems.ch/MFISC/VAUD/sipf-bof</scm.url>
            </properties>

            <distributionManagement>
                <repository>
                    <id>central</id>
                    <name>Artifactory-releases</name>
                    <url>https://artifactrepo.cross-systems.ch/artifactory/sipf-release/</url>
                </repository>
                <snapshotRepository>
                    <id>snapshots</id>
                    <name>Artifactory-snapshots</name>
                    <url>https://artifactrepo.cross-systems.ch/artifactory/sipf-snapshot/</url>
                </snapshotRepository>
            </distributionManagement>
        </profile>
    </profiles>
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-version}</version>
                <executions>
                    <execution>
                        <id>default</id>
                        <goals>
                            <goal>prepare-agent</goal>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
