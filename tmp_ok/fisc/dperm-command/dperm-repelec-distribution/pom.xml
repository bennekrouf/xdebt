<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>ch.vd.fiscalite.dperm</groupId>
        <artifactId>dperm-repelec</artifactId>
        <version>24R2.B.1-SNAPSHOT</version>
    </parent>
    <artifactId>dperm-repelec-distribution</artifactId>
    <packaging>pom</packaging>

    <properties>
        <application.name>dperm-command</application.name>
        <val.name.application>${application.name}-${project.version}-val-${maven.build.timestamp}</val.name.application>

        <dist.app.name>dperm-command-${project.version}</dist.app.name>
        <dist.app.link.name>dperm-command</dist.app.link.name>
        <dist.app.dir>${dist.build.dir}/${dist.app.name}</dist.app.dir>
        <dist.final.name.dep>deployment</dist.final.name.dep>
        <dist.final.name.cfg>config</dist.final.name.cfg>

        <dist.build.dir>${project.build.directory}/dist</dist.build.dir>
        <local.distribution.artifact.file>${project.build.directory}/${dist.app.name}.zip
        </local.distribution.artifact.file>
        <file.mode.release>0644</file.mode.release>


        <xfile.upload.from>${user.name}</xfile.upload.from>
    </properties>

    <dependencies>
        <dependency>
            <groupId>ch.vd.fiscalite.dperm</groupId>
            <artifactId>dperm-repelec-configuration</artifactId>
            <version>${project.version}</version>
            <type>zip</type>
        </dependency>
        <dependency>
            <groupId>ch.vd.fiscalite.dperm</groupId>
            <artifactId>dperm-repelec-command</artifactId>
            <type>war</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>copy-dependencies-distribution</id>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>dperm-repelec-command</artifactId>
                                    <version>${project.version}</version>
                                    <type>war</type>
                                    <outputDirectory>${project.build.directory}/dist/dperm-command/deployment
                                    </outputDirectory>
                                    <destFileName>dperm-command.war</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unpack-configuration</id>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <outputDirectory>${project.build.directory}/dist</outputDirectory>
                            <includeArtifactIds>dperm-repelec-configuration</includeArtifactIds>
                            <includeGroupIds>${project.groupId}</includeGroupIds>
                            <excludeTransitive>true</excludeTransitive>
                            <excludeTypes>pom</excludeTypes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                    <configuration>
                        <skip>false</skip>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>