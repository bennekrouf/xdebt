<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>ch.vd.tools</groupId>
    <artifactId>pingpong</artifactId>
    <version>4.0.6-SNAPSHOT</version>
    <packaging>jar</packaging>

    <parent>
        <groupId>ch.vd.devsupport</groupId>
        <artifactId>vd-application-parent</artifactId>
        <version>2.3.0</version>
        <relativePath />
    </parent>

    <scm>
        <developerConnection>scm:git:git@bitbucket.etat-de-vaud.ch::/tools/pingpong</developerConnection>
        <tag>HEAD</tag>
    </scm>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.vd.devsupport</groupId>
            <artifactId>devsupport-authentication-starter</artifactId>
            <version>${devsupport.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.vd.devsupport</groupId>
            <artifactId>devsupport-spring-utils</artifactId>
            <version>${devsupport.version}</version>
        </dependency>

        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Deps de test -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.2.18</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <compilerArg>
                            -Amapstruct.defaultComponentModel=spring
                        </compilerArg>
                    </compilerArgs>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <properties>
        <java.version>21</java.version>

        <devsupport.version>3.3.0</devsupport.version>

        <release.goals>clean deploy</release.goals>
        <release.profiles>not,assembly,nexus</release.profiles>

        <devex-maven-plugin.deployUnit.IN>pingpong2</devex-maven-plugin.deployUnit.IN>
        <devex-maven-plugin.deployTarget.IN>Tomcat_RHEL6</devex-maven-plugin.deployTarget.IN>
        <devex-maven-plugin.deployUnit.I2>pingpong1</devex-maven-plugin.deployUnit.I2>
        <devex-maven-plugin.deployTarget.I2>Tomcat_RHEL7</devex-maven-plugin.deployTarget.I2>
        <devex-maven-plugin.configTag>master</devex-maven-plugin.configTag>

        <t6kt-maven-plugin.deployUnit>pingpong2</t6kt-maven-plugin.deployUnit>

        <tesserakt-maven-plugin.url>https://int-esgate.etat-de-vaud.ch/tesserakt-in</tesserakt-maven-plugin.url>
        <T6KT_USERNAME>${T6KT_IN_USERNAME}</T6KT_USERNAME>
        <T6KT_PASSWORD>${T6KT_IN_PASSWORD}</T6KT_PASSWORD>
    </properties>
</project>
