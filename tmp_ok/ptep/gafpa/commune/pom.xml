<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

	<artifactId>gafpa-commune</artifactId>
    <version>5.1.2-SNAPSHOT</version>
    <packaging>war</packaging>

    <name>Prestation - Gafpa - Commune</name>

    <parent>
        <groupId>ch.vd.cyber.parent</groupId>
        <artifactId>application-parent</artifactId>
        <version>3.5.11</version>
        <relativePath />
    </parent>
    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>gafpa-jaxb</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>gafpa-common</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-sleuth</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-config</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-eureka</artifactId>
        </dependency>
        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
        </dependency>

        <!-- Deps de test -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>gafpa-jaxb</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
            <type>test-jar</type>
        </dependency>
        <dependency>
            <groupId>ch.vd.cyber</groupId>
            <artifactId>starter-bo-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.4.0</version>
                <executions>
                    <execution>
                        <id>exec-yarn-install</id>
                        <configuration>
                            <executable>yarn</executable>
                            <workingDirectory>front</workingDirectory>
                            <arguments>
                                <argument>install</argument>
                            </arguments>
                            <skip>${build.nofront}</skip>
                        </configuration>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>exec-bower-install</id>
                        <configuration>
                            <executable>bower</executable>
                            <workingDirectory>front</workingDirectory>
                            <arguments>
                                <argument>install</argument>
                            </arguments>
                            <skip>${build.nofront}</skip>
                        </configuration>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>exec-gulp-build</id>
                        <configuration>
                            <executable>yarn</executable>
                            <arguments>
                                <argument>build</argument>
                            </arguments>
                            <workingDirectory>front</workingDirectory>
                            <skip>${build.nofront}</skip>
                        </configuration>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                    </execution>
                    <!--<execution>-->
                        <!--<id>exec-gulp-test</id>-->
                        <!--<configuration>-->
                            <!--<executable>gulp</executable>-->
                            <!--<workingDirectory>front</workingDirectory>-->
                            <!--<arguments>-->
                                <!--<argument>test</argument>-->
                            <!--</arguments>-->
                            <!--<skip>${skipTests}</skip>-->
                        <!--</configuration>-->
                        <!--<phase>package</phase>-->
                        <!--<goals>-->
                            <!--<goal>exec</goal>-->
                        <!--</goals>-->
                    <!--</execution>-->
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>${maven-resources-plugin.version}</version>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <skip>${build.nofront}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>copy-gulp-dist</id>
                        <!-- here the phase you need -->
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${basedir}/target/classes/static</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>front/dist</directory>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <properties>
        <start-class>ch.vd.gafpa.GafpaComApplication</start-class>

        <webit.port>15600</webit.port>
        <webit.applicationName>gafpacom</webit.applicationName>
        <webit.appDir>../appDir/CI</webit.appDir>

        <assembly.releaseName>gafpacom</assembly.releaseName>
        <assembly.finalName>${webit.applicationName}-${project.version}</assembly.finalName>
    </properties>
</project>
