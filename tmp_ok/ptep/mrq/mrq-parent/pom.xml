<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>ch.vd.ptep</groupId>
    <artifactId>mrq-parent</artifactId>
    <version>0.4.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>MRQ - Parent for mrq libraries</name>

    <scm>
        <developerConnection>scm:git:git@bitbucket.etat-de-vaud.ch::/PTEP/mrq</developerConnection>
        <tag>HEAD</tag>
    </scm>

    <parent>
        <groupId>ch.vd.devsupport</groupId>
        <artifactId>devsupport-library-parent</artifactId>
        <version>3.5.0</version>
        <relativePath />
    </parent>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <skipIfEmpty>true</skipIfEmpty>
                    <skip>${skip-test-jar}</skip>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                        <phase>verify</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <configuration>
                    <suppressionsLocation>checkstyle-suppressions.xml</suppressionsLocation>
                </configuration>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <configuration>
                            <skip>false</skip>
                            <configLocation>${maven-checkstyle-plugin.configLocation}</configLocation>
                            <consoleOutput>true</consoleOutput>
                            <failsOnError>true</failsOnError>
                        </configuration>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencyManagement>
        <dependencies>
            <!-- self -->
            <dependency>
                <groupId>ch.vd.ptep</groupId>
                <artifactId>mrq-model</artifactId>
                <version>0.4.0-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>ch.vd.ptep</groupId>
                <artifactId>mrq</artifactId>
                <version>0.4.0-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>ch.vd.ptep</groupId>
                <artifactId>mrq-compiler</artifactId>
                <version>0.4.0-SNAPSHOT</version>
            </dependency>

            <!-- Sibatcore -->
            <dependency>
                <groupId>ch.vd.ptep</groupId>
                <artifactId>sibatcore-jaxb</artifactId>
                <version>${sibatcore-jaxb.version}</version>
            </dependency>

            <!-- Others -->
            <dependency>
                <groupId>org.jetbrains</groupId>
                <artifactId>annotations</artifactId>
                <version>${jetbrains-annotations.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${org.mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <properties>
        <java.version>21</java.version>
        <skip-test-jar>true</skip-test-jar>
        <maven.deploy.skip>false</maven.deploy.skip>
        <release.pushChanges>true</release.pushChanges>

        <sibatcore-jaxb.version>0.4.0</sibatcore-jaxb.version>
        <org.mapstruct.version>1.5.5.Final</org.mapstruct.version>
        <jetbrains-annotations.version>24.0.1</jetbrains-annotations.version>
        <guava.version>33.2.1-jre</guava.version>
    </properties>
</project>
