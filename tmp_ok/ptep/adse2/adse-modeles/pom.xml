<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>ch.vd.ptep.adse</groupId>
		<artifactId>adse</artifactId>
		<version>14.1.4-SNAPSHOT</version>
	</parent>

	<artifactId>adse-modeles</artifactId>
	<packaging>pom</packaging>
	<name>ADSE Modeles</name>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.gmaven</groupId>
				<artifactId>groovy-maven-plugin</artifactId>
				<executions>
					<execution>
						<phase>verify</phase>
						<goals>
							<goal>execute</goal>
						</goals>
						<configuration>
							<source>
								<![CDATA[
		def containsNonAscii(String s) {
		  s.toCharArray().find { it > 128 } != null
		}
		
		def files = new File(project.basedir, "ModelesCourrier").listFiles().findAll{containsNonAscii(it.getAbsolutePath())}
		if (files.size() > 0) {
		    println("les noms des fichiers suivants ont des caractères speciaux, il faut les remplacer avec des caractères ascii");
		    for (f in files) {
			    println("-- " + f.getAbsolutePath())
			}
            fail("nom de fichiers modèles courriers contiennent des caractères spéciaux");
		}
        ]]></source>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>

				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>

						<configuration>
							<descriptors>
								<descriptor>${project.basedir}/assembly-distribution.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
